var splide = new Splide( '.splide', {
    perPage: 3,
    loop: true,
    rewind: true,
    arrows: false,
    wheel: true,
    autoplay: true,
    interval: 3000,
    speed: 3000,
  
    
  } );
  
  splide.mount();